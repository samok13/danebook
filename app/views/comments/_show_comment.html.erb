
<%= link_to author(comment), user_timeline_path(comment.user_id) %> created at <%= comment.created_at %>:
<br/>

<%= comment.body %>
<br/>

<% if current_user && current_user.id == comment.user_id %>
  <%= link_to "Delete", user_comment_path(comment.user_id, comment), method: :delete %>
<% end %>
