
<%= link_to author(comment), posts_path(comment.user_id) %> commented at <%= comment.created_at %>:
<br/>

<%= comment.body %>
<br/>

<% if current_user && current_user.id == comment.user_id %>
  
  <%= like_status(comment) %>

  <%= link_to "Delete", comment_path(comment), method: :delete %>
<% end %>
